<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
        
<mapper namespace="com.project.bookmarkboard.mapper.SearchMapper">
    <select id="getAllFromMyFolder" resultType="com.project.bookmarkboard.dto.FolderDTO">
        SELECT * FROM folder WHERE title like concat('%', #{searchkeyword}, '%') AND owner = #{owner}
    </select>
    
	<select id="getAllFromOurFolder" resultType="com.project.bookmarkboard.dto.FolderDTO">
        SELECT * FROM folder WHERE title like concat('%', #{searchkeyword}, '%') AND is_shared = 1
    </select>
</mapper>